# Browser (Vite) — public anon key is safe to expose in the frontend
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server-only — used by scripts/import_courses.js (never commit the real .env)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
